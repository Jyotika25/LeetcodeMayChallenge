class Solution {
public:
    bool isPerfectSquare(int num) {
        
        long long i =1;
        
        for(i=1;i*i<= num;i++)
        {
            if(num%i==0 && num/i==i)
                return true;
        }
        return false;
    }
};


//16%4=0 and 16/4=4
